feat: styled confirmation modals, custom file picker, hanko favicon

UI polish pass replacing the remaining raw-browser chrome with
design-system equivalents. Frontend-only, no schema or API changes.

js/app.js
- NEW appConfirm({title, message, okLabel, danger}): promise-based
  styled confirmation dialog — washi card, indigo top border (shu red
  for danger variant), Escape/backdrop-click to cancel, subtle pop
  animation, focus lands on the confirm button

project.html
- Native confirm() replaced with appConfirm for: document deletion,
  password reset, member removal (the last one previously had no
  confirmation at all)
- File input restyled: native control visually hidden (kept keyboard-
  accessible with a focus ring), replaced by a 📁 Choose button +
  selected-filename readout ("2 件のファイル" for multi-select),
  resets after upload

viewer.html
- Comment deletion uses appConfirm (message now notes replies are
  deleted too)

favicon.svg (NEW) + all pages
- Hanko-style favicon: rotated shu-vermillion seal with 朱 character,
  SVG so it stays crisp at any size; <link rel="icon"> added to all
  five pages

css/style.css
- .modal-backdrop/.modal styles + animations, .file-picker styles

build.sh
- Copies favicon.svg into dist/
